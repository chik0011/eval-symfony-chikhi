<form method="post" action="{{ path('demande_delete', {'id': c_request.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ c_request.id) }}">
    <button class="btn">Delete</button>
</form>
